<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_info_details"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReclaimUserApp">
        <activity
            android:name=".screens.activities.ReportItemActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.ItemDetailActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
